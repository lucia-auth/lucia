{
	"name": "@lucia-auth/session-adapter-redis",
	"version": "0.1.0",
	"description": "Redis session adapter for Lucia",
	"main": "index.js",
	"types": "index.d.ts",
	"module": "index.js",
	"type": "module",
	"files": [
		"**/*"
	],
	"scripts": {
		"build": "rm -rf ./dist/* && tsc && cp ./package.json ./dist && cp ./README.md ./dist && cp .npmignore dist",
		"test-main": "npx ts-node --esm -T test/index.ts",
		"package": "pnpm build && cd dist && pnpm prune && pnpm publish --no-git-checks --access public && cd ../"
	},
	"keywords": [
		"lucia",
		"redis",
		"lucia-auth"
	],
	"repository": {
		"type": "git",
		"url": "https://github.com/pilcrowOnPaper/lucia-auth",
		"directory": "packages/adapter-supabase"
	},
	"author": "pilcrowonpaper",
	"license": "MIT",
	"exports": {
		".": "./index.js"
	},
	"peerDependencies": {
		"lucia-auth": "~0.14.3"
	},
	"devDependencies": {
		"@lucia-auth/adapter-test": "workspace:*",
		"dotenv": "^16.0.3",
		"redis": "^4.3.1",
		"ts-node": "^10.9.1"
	},
	"dependencies": {
		"lucia-auth": "workspace:*"
	}
}
