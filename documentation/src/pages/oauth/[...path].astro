---
import DocumentLayout from "@layouts/DocumentLayout.astro";
import OAuthContentTable from "@components/content-table/OAuthContentTable.astro";
import { type Page, handleGetStaticPaths } from "src/lib/content";
import type { GetStaticPaths } from "astro";

export const getStaticPaths: GetStaticPaths = handleGetStaticPaths(
	await Astro.glob("/content/oauth/*/*.md")
);

type Props = {
	page: Page;
};

const { page } = Astro.props;
---

<DocumentLayout title={page.title} headings={page.getHeadings()} ContentTable={OAuthContentTable} external="oauth">
	<page.Content />
</DocumentLayout>
