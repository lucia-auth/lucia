---
import DocumentLayout from "@layouts/DocumentLayout.astro";
import NextjsContentTable from "@components/content-table/NextjsContentTable.astro";
import { type Page, handleGetStaticPaths } from "src/lib/content";
import type { GetStaticPaths } from "astro";

export const getStaticPaths: GetStaticPaths = handleGetStaticPaths(
	await Astro.glob("/content/nextjs/*/*.md")
);

type Props = {
	page: Page;
};

const { page } = Astro.props;
---

<DocumentLayout title={page.title} headings={page.getHeadings()} ContentTable={NextjsContentTable} external="nextjs">
	<page.Content />
</DocumentLayout>
